"use server";
import { revalidateTag } from "next/cache";

//All revalidations should be placed here
/*e.g export async function revalidateBlog() {
revalidateTag("blog");
revalidateTag("blog-${slug}")
}
*/
